Everytime you make a change to a file, replace the namespace scopes with file-scoped namespace declarations.

Prefer using early returns to reduce nesting and improve readability.

Prefer using the specific object types instead of var

When adding debug print statements, use System.Diagnostics.Debug.WriteLine instead of Console.WriteLine for better control over debug output.